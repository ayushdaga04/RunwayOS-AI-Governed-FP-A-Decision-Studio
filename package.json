{
  "name": "ai-fpa-workforce-planner",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "predev": "node bundle.mjs",
    "dev": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node bundle.mjs && node make-standalone.mjs && node build.mjs"
  }
}
